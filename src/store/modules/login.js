

const state = {
    VerifiCode:''
}

const actions = {
    // getVerifiCode(context, value){//获取验证码
    //     return new Promise((resolve, reject) => {
    //         request({
    //             method:get,
    //             url:'',
    //             responseType:'json'
    //         }).then(response => {
    //             re
    //         })
    //     }).catch(error => {
    //         reject(error)
    //     })
    // }
}

const mutations = {

}

export default {
    namespaced: true,
    state,
    mutations,
    actions
  }